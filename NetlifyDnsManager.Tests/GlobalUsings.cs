global using Microsoft.VisualStudio.TestTools.UnitTesting;

// The fixtures every test class builds on, so that each of them does not repeat the same using
global using NetlifyDnsManager.Tests.TestSupport;
